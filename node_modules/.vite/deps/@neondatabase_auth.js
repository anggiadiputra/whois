import {
  AuthApiError,
  AuthError,
  BetterAuthVanillaAdapter,
  isAuthApiError,
  isAuthError
} from "./chunk-7LQJ63VI.js";
import "./chunk-DC5AMYBS.js";

// node_modules/@neondatabase/auth/dist/neon-auth-BEYvHA5c.mjs
function createInternalNeonAuth(url, config) {
  const adapterBuilder = (config == null ? void 0 : config.adapter) ?? BetterAuthVanillaAdapter();
  const { fetchOptions } = config ?? {};
  const adapter = adapterBuilder(url, fetchOptions);
  const allowAnonymous = (config == null ? void 0 : config.allowAnonymous) ?? false;
  if (!(typeof adapter.initialize === "function")) return {
    getJWTToken: () => adapter.getJWTToken(allowAnonymous),
    adapter: adapter.getBetterAuthInstance()
  };
  return {
    getJWTToken: () => adapter.getJWTToken(allowAnonymous),
    adapter
  };
}
function createAuthClient(url, config) {
  return createInternalNeonAuth(url, config).adapter;
}
export {
  AuthApiError,
  AuthError,
  createAuthClient,
  createInternalNeonAuth,
  isAuthApiError,
  isAuthError
};
//# sourceMappingURL=@neondatabase_auth.js.map
